import { ArrowRight, MessageCircle, X } from 'lucide-react'

export default function TravelChatbot({ open, messages, input, loading, error, onInputChange, onSend, onClose, onOpen }) {
  if (!open) return <button className="assistant-button" onClick={onOpen} aria-label="Open AI travel assistant"><MessageCircle size={21} /><span>Ask Wayfarer</span></button>
  return <div className="modal-backdrop" onClick={onClose}><aside className="planner-modal" onClick={(event) => event.stopPropagation()}><button className="close-button" onClick={onClose} aria-label="Close assistant"><X /></button><div className="planner-modal-head"><MessageCircle size={20} /><p className="eyebrow">Wayfarer assistant</p><h2>Ask about<br /><em>anywhere.</em></h2></div><div className="chat-messages">{messages.map((message, index) => <p className={message.role} key={`${message.role}-${index}`}>{message.text}</p>)}{loading && <p className="assistant" role="status">Wayfarer is thinking...</p>}{error && <p className="assistant chat-error" role="alert">{error}</p>}</div><div className="chat-compose"><input value={input} onChange={(event) => onInputChange(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && onSend()} placeholder="Ask about a destination..." aria-label="Ask Wayfarer a question" /><button className="button button-dark" onClick={onSend} disabled={loading} aria-label="Send question"><ArrowRight size={17} /></button></div></aside></div>
}
